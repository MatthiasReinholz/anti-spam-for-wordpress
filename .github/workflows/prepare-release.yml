name: prepare-release

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Release version in x.y.z format'
        required: true
        type: string
      base_ref:
        description: 'Base branch for the release branch'
        required: true
        default: 'main'
        type: string

permissions:
  contents: write
  pull-requests: write

concurrency:
  group: prepare-release-${{ inputs.version }}
  cancel-in-progress: false

jobs:
  prepare:
    name: Prepare release branch
    runs-on: ubuntu-latest

    steps:
      - name: Checkout base ref
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          ref: ${{ inputs.base_ref }}

      - name: Validate version
        run: |
          if ! [[ "${{ inputs.version }}" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "Version must use x.y.z format." >&2
            exit 1
          fi

      - name: Ensure release branch does not already exist
        env:
          RELEASE_BRANCH: release/${{ inputs.version }}
        run: |
          if git ls-remote --exit-code --heads origin "$RELEASE_BRANCH" >/dev/null 2>&1; then
            echo "Release branch $RELEASE_BRANCH already exists. Update the existing release PR manually or delete the branch before rerunning prepare-release." >&2
            exit 1
          fi

      - name: Bump release metadata
        run: bash scripts/release/bump_version.sh "${{ inputs.version }}"

      - name: Create or update release pull request
        uses: peter-evans/create-pull-request@22a9089034f40e5a961c8808d113e2c98fb63676
        with:
          branch: release/${{ inputs.version }}
          base: ${{ inputs.base_ref }}
          commit-message: Prepare release ${{ inputs.version }}
          title: Prepare release ${{ inputs.version }}
          body: |
            This PR was created by the `prepare-release` workflow.

            Included changes:
            - bump plugin metadata to `${{ inputs.version }}`
            - add a changelog stub for `${{ inputs.version }}`
            - update the POT project version

            Release checklist:
            - [ ] Replace the changelog placeholder with final release notes.
            - [ ] Confirm the version bump is correct in plugin metadata and `readme.txt`.
            - [ ] Confirm CI passed on this release PR.
            - [ ] Complete a WordPress smoke test:
              - [ ] settings page loads
              - [ ] widget renders
              - [ ] challenge solve succeeds
              - [ ] verification succeeds
              - [ ] one representative integration still works
            - [ ] Merge this PR into `main`.

            After merge:
            - the `finalize-release` workflow creates and pushes the annotated `${{ inputs.version }}` tag automatically
            - the tag triggers the `release` workflow automatically
          delete-branch: false
